apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: model-royale
spec:
  to:
    kind: Service
    name: web
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
