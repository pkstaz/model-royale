apiVersion: v1
kind: ConfigMap
metadata:
  name: model-royale-config
data:
  MOCK_INFERENCE: "false"
  CORS_ORIGINS: "*"
  # Los modelos NO van aquí. El admin pega la URL de cada InferenceService en el mantenedor.
